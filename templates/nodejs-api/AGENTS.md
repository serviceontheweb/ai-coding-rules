# Node.js API Agent Notes

Start with `core/AGENTS.md`, then add project-specific facts below.

## Project Facts

- Runtime:
- Package manager:
- API framework:
- Test command:
- Lint command:
- Build command:

## Local Rules

- Validate request input at API boundaries.
- Keep database access parameterized.
- Do not log tokens, credentials, personal data, or full request payloads by
  default.
- Add focused tests for route behavior and service logic.

## Unknowns

- Document unknown commands or deployment details here instead of guessing.
